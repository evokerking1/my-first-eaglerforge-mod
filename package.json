{
  "name": "Click notifier",
  "version": "1.0.0",
  "description": "Example typescript mod for eaglerforge.",
  "scripts": {
    "build": "bun build --bundle ./src/main.ts --outdir=MODS/ --outfile=out/plugin.js --sourcemap=inline --watch --minify"
  },
  "author": "evokerking",
  "devDependencies": {
    "css-loader": "^6.8.1",
    "ts-loader": "^9.4.4"
  }
}
